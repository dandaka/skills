{
  "name": "real-browser",
  "description": "Attach agent-browser to a real, human-owned Chrome (persistent logins, one profile per CDP port, --pin-tab per session) instead of launching a Playwright-controlled browser. Use for sites that detect automation, QA against a logged-in app, or parallel agents sharing one browser.",
  "version": "2.0.0",
  "userInvocable": true
}
